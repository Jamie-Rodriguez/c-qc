# Assemble the design spec and remaining ADRs

Type: task
Status: open
Blocked by: 07, 10, 12, 13

## Question

Gather every closed ticket's answer into one design spec at `docs/design.md`: public API, supervisor and worker model and protocol, typed choice model, shrinker, generator set, sanitizer integration, reporting, build and source layout, portability strategy, and the test plan for the library itself. Write any further ADRs the decisions warrant. Resolved when the spec is sharp enough that the first implementation ticket could be written without a design question.
