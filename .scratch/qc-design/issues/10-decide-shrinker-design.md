# Decide the shrinker design

Type: grilling
Status: open
Blocked by: 04, 08

## Question

Given the findings on Hypothesis's shrinker and `qc`'s choice model, decide which shrink passes `qc` implements, in what order, what ordering it minimises toward, the stopping rule, how a crash or timeout during a shrink attempt is treated relative to the original failure, and how the cost of shrinking hangs is bounded.
